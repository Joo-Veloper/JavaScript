---
# JavaScript 기본 및 변수 명명 규칙
---

## 시작하기

```javascript
let js = "amazing";
```

위 코드는 `js`라는 변수를 선언하고 문자열 값 `"amazing"`을 할당합니다. 이후 조건문에서 이 변수의 값을 확인하여 코드의 동작을 결정할 수 있습니다.

---

## 조건문을 이용한 경고창 출력

```javascript
if (js === "amazing") {
  alert("JavaScript is Fun!"); // alert창
}
```

`js` 변수의 값이 `"amazing"`일 때 `alert` 함수를 통해 "JavaScript is Fun!"이라는 메시지를 경고창으로 표시합니다. 하지만 이 코드는 주석 처리되어 있어 실행되지 않습니다.

---

## 콘솔 로그 사용하기

```javascript
console.log(40 + 8 + 23 - 10);
console.log("Jonas");
console.log(23);
```

`console.log`를 사용해 계산 결과나 텍스트, 숫자를 브라우저의 콘솔에 출력할 수 있습니다. 이는 디버깅이나 프로그램의 상태를 확인할 때 유용합니다.

---

## 변수 선언과 명명 규칙

### 기본 변수 선언 예시

```javascript
let firstName = "Matilda"; // 변수를 사용하면 값 변경이 용이함
let first = "jonas";
```

`firstName`과 같은 변수를 선언하여 값을 할당하면, 필요할 때마다 해당 변수를 호출해 값을 사용할 수 있어 유지보수에 편리합니다.

### 변수 명명 규칙

JavaScript 변수는 다음과 같은 규칙을 따라야 합니다:

- **CamelCase** 또는 **언더스코어**(`_`)를 사용해 여러 단어를 연결하는 것이 일반적입니다.

```javascript
let firstNamePerson;
let first_name_person;
```

### 변수 이름에 대한 주의 사항

- 변수 이름은 숫자로 시작할 수 없습니다.

  ```javascript
    let 3year = 3; // 오류 발생
  ```

- 변수 이름에 특수 문자를 사용할 수 없습니다(예: `&`).

  ```javascript
    let jonas&matilda = 'JM'; // 오류 발생
  ```

- JavaScript의 예약어는 변수 이름으로 사용할 수 없습니다.

  ```javascript
    let new = 27; // 오류 발생
  ```

  예외적으로, 예약어와 유사한 이름은 사용 가능합니다.

  ```javascript
  let sfunction = 27; // 사용 가능
  ```

- JavaScript에서 `name`은 예약어는 아니지만, 일반적으로 `firstName`과 같은 명확한 변수 이름을 사용하는 것이 좋습니다.

---

## 상수 선언

변하지 않는 값에는 대문자 변수명을 사용하는 것이 관례입니다. 예를 들어, 원주율 값은 아래와 같이 선언합니다:

```javascript
let PI = 3.1415;
```

---

## 변수명을 설명적으로 작성하기

변수명을 작성할 때는 변수의 용도를 쉽게 알 수 있도록 설명적으로 작성하는 것이 좋습니다.

### 예시

```javascript
// 1번 방법 - 설명적 변수명 사용
let myFirstJob = "Programmer";
let myCurrentJob = "Teacher";

// 2번 방법 - 설명 부족한 변수명 사용
let job1 = "Programmer";
let job2 = "Teacher";
```

위 예시에서 1번 방법이 더 설명적이므로, 코드를 읽는 사람이 변수의 역할을 쉽게 파악할 수 있습니다.

---

## 실행 예시

코드를 실행하면 다음과 같이 콘솔에 결과가 출력됩니다:

```javascript
console.log(myFirstJob); // "Programmer"
console.log(firstName); // "Matilda"
```

# JavaScript 연산자 동작 이해

## JavaScript는 **암시적 형 변환(Implicit Type Coercion)**을 통해 데이터 타입을 자동으로 변환합니다.

## 코드 및 설명

### 1. `"9" - "5"`

```javascript
console.log("9" - "5"); // 결과: 4
```

- 문자열 `"9"`와 `"5"`가 숫자로 변환된 뒤 뺄셈(`-`) 연산이 수행됩니다.
- 결과: `9 - 5 = 4`

---

### 2. `"19" - "13" + "17"`

```javascript
console.log("19" - "13" + "17"); // 결과: "617"
```

- `"19" - "13"`: 문자열 `"19"`와 `"13"`이 숫자로 변환되어 `19 - 13 = 6`이 됩니다.
- `6 + "17"`: 숫자 `6`이 문자열 `"6"`으로 변환되고, `"6" + "17"`은 문자열 연결로 `"617"`이 됩니다.
- 결과: `"617"`

---

### 3. `"19" - "13" + 17`

```javascript
console.log("19" - "13" + 17); // 결과: 23
```

- `"19" - "13"`: 문자열 `"19"`와 `"13"`이 숫자로 변환되어 `19 - 13 = 6`이 됩니다.
- `6 + 17`: 숫자끼리의 덧셈 연산이 수행되어 `6 + 17 = 23`이 됩니다.
- 결과: `23`

---

### 4. `"123" < 57`

```javascript
console.log("123" < 57); // 결과: false
```

- 비교 연산자 `<`는 문자열 `"123"`을 숫자로 변환합니다.
- 숫자 `123`과 `57`을 비교하여 `123 < 57`은 **`false`**입니다.
- 결과: `false`

---

### 5. `5 + 6 + "4" + 9 - 4 - 2`

```javascript
console.log(5 + 6 + "4" + 9 - 4 - 2); // 결과: 1143
```

1. `5 + 6`: 숫자끼리의 덧셈으로 `11`이 됩니다.
2. `11 + "4"`: 숫자 `11`이 문자열 `"11"`로 변환되고, `"11" + "4"`는 문자열 연결로 `"114"`가 됩니다.
3. `"114" + 9`: 문자열 `"114"`가 숫자 `9`와 연산되면서 암시적 형 변환이 일어나지 않고 문자열 연결로 `"1149"`가 됩니다.
4. `"1149" - 4`: 문자열 `"1149"`가 숫자로 변환되어 `1149 - 4 = 1145`가 됩니다.
5. `1145 - 2`: 숫자끼리의 뺄셈으로 `1143`이 됩니다.

- 결과: `1143`

---

## 결론

JavaScript의 연산자는 다음과 같은 규칙을 따릅니다:

1. **산술 연산자 (`-`, `*`, `/`)**:
   - 문자열을 숫자로 변환한 뒤 계산합니다.
2. **`+` 연산자**:
   - 한쪽이 문자열이면 **문자열 연결**을 수행합니다.
3. **비교 연산자 (`<`, `>`)**:
   - 문자열이 숫자와 비교될 경우, 문자열을 숫자로 변환합니다.
4. **혼합 연산**:
   - 연산 순서에 따라 타입 변환 및 결과가 달라질 수 있습니다.

## 자바스크립트에서 변수를 선언하는 방법으로 **`const`**, **`let`**, **`var`**를 사용할 수 있습니다. ES6에서 도입된 `let`과 `const`는 현대적인 자바스크립트에서 주로 사용되며, `var`는 이전 방식으로 현재는 거의 사용되지 않습니다.

### 1. **`const` (상수 선언)**

- **특징:**

  - 선언과 동시에 초기값을 할당해야 합니다.
  - **값을 재할당할 수 없습니다.** (불변성)
  - 블록 범위를 가집니다. (Block Scope)
  - 객체나 배열의 속성은 변경할 수 있지만, 참조 자체는 변경할 수 없습니다.

- **적합한 상황:**

  - **절대 변경되지 않을 값**을 선언할 때.
  - 예: 출생 연도, 상수 값 등.

- **예제:**
  ```javascript
  const birthYear = 1991;
  // birthYear = 1990; // TypeError: Assignment to constant variable
  ```

---

### 2. **`let` (변수 선언)**

- **특징:**

  - 선언 후 값을 나중에 재할당할 수 있습니다.
  - 블록 범위를 가집니다. (Block Scope)
  - 선언 전에 접근하면 `ReferenceError`가 발생합니다.

- **적합한 상황:**

  - 값이 **동적으로 변경될 가능성이 있는 경우**.
  - 예: 사용자 입력 데이터, 상태값 등.

- **예제:**
  ```javascript
  let age = 30;
  age = 31; // 가능
  ```

---

### 3. **`var` (이전 방식, 권장되지 않음)**

- **특징:**

  - **함수 범위**를 가집니다. (Function Scope)
  - 재선언이 가능하여 의도치 않은 값 덮어쓰기가 발생할 수 있습니다.
  - 선언 전에 접근하면 `undefined`로 초기화됩니다. (호이스팅)
  - 현대 코드에서는 사용을 지양합니다.

- **적합한 상황:**

  - ES6 이전의 코드와의 호환성을 위해 사용.
  - 새로운 코드에서는 사용하지 않는 것이 좋습니다.

- **예제:**
  ```javascript
  var job = "programmer";
  job = "developer"; // 가능
  ```

---

### 4. **`const`와 `let`을 사용하는 모범 사례**

- **기본적으로 `const`를 사용**합니다.
  - 불필요한 변수의 변경을 방지하고, 코드의 안정성을 높입니다.
- **값이 변할 필요가 있을 때만 `let`을 사용**합니다.
  - 변경 가능한 변수 사용을 명시적으로 표현하여 코드 가독성을 높입니다.

---

### 5. **정리된 차이점 표**

| 구분          | `const`               | `let`                 | `var`                                   |
| ------------- | --------------------- | --------------------- | --------------------------------------- |
| **값 재할당** | 불가능                | 가능                  | 가능                                    |
| **범위**      | 블록 범위             | 블록 범위             | 함수 범위                               |
| **호이스팅**  | 초기화 없이 사용 불가 | 초기화 없이 사용 불가 | 초기화된 값으로 사용 가능 (`undefined`) |
| **사용 권장** | 상수 값               | 변경될 값             | 사용 지양                               |

---

### 6. **비추천: 선언 없이 변수 사용**

- 선언하지 않고 변수명을 사용하면 전역 객체(`window` 또는 `global`)의 속성으로 추가됩니다.
- **버그의 원인**이 될 수 있으므로 피해야 합니다.
- **잘못된 예제:**
  ```javascript
  name = "JavaScript"; // 선언 없이 사용 (비추천)
  console.log(name); // "JavaScript"
  ```

---

### 결론

- **`const`를 기본적으로 사용**하고, 값이 변할 필요가 있는 경우에만 `let`을 사용합니다.
- **`var`는 사용하지 않는 것이 좋습니다.**
